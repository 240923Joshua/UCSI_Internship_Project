# UCSI_Internship_Project
**AI Avatar based internship guide / mentor application**

## Backend stuffs
Run `schema.sql` before using these files so that the database will be created  
Use Python 3.10.6 to have the same version as mine  
Use a python virtual environment to ensure it's exactly same as mine `Use python -m venv <folder_name>` and `<folder_name>/scripts/activate`  
Run `pip install requirements.txt` to have exact versions and modules as mine  
